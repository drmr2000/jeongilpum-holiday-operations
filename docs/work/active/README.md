# Active work claims

진행 중인 작업자는 이 폴더에 `YYYYMMDD-<owner>-<task>.md` 파일을 만든다.

작업 전 이 폴더의 모든 파일에서 `Claimed paths`와 `Shared contracts`를 확인한다. README 자체에 작업 목록을 추가하지 않는다. 개발자별 고유 파일을 사용해야 중앙 파일 merge conflict를 피할 수 있다.
